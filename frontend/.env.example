# ============================================================
# Variables de entorno del frontend — copiar este archivo a
# frontend/.env. NUNCA subir el .env real a Git.
# ============================================================

# URL base del backend (debe coincidir con PORT en backend/.env)
VITE_API_URL=http://localhost:4000/api
